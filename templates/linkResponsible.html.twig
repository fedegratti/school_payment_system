<!DOCTYPE html>
<html>
  <head> 
    <title>Asociar responsable</title>
    <link rel="stylesheet" href="../styles/style.css">
  </head>
  <body>
    <h1>Asociar responsable</h1>
    <div class="whiteBox">
      {% if responsibles|length > 0 %}
      <table class="table">
        <tbody>
          <tr class="even">
            <th>Nombre y Apellido</th>
            <th>Acci&oacute;n</th>
          </tr>
          {% for responsible in responsibles %}
          <tr class="odd">
            <td>{{ responsible[1] }}</td>
            <td><a href="asociateResponsibleAction/{{ responsible[0] }}/{{ studentID }}">Seleccionar</a></td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
      {% endif %}
    </div>
  </body>
</html>